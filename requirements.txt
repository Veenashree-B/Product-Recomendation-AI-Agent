# core
streamlit==1.37.0
numpy==2.3.5
pandas==2.2.3

# rest of your deps (keep the same order if possible)
openai==1.3.5
python-dotenv==1.0.0
sentence-transformers==2.3.0
chromadb==0.4.10
# add setuptools and wheel to help builds
setuptools>=67.0.0
wheel
